import React from 'react';

import { Button } from 'component-library';

interface HomeProps { }

const Home: React.FC<HomeProps> = () => {
    return (
        <React.Fragment>
            <Button />
        </React.Fragment>
    );
}

export default Home;
